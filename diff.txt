diff --git a/packages/web-client/public/js/physics-engine.js b/packages/web-client/public/js/physics-engine.js
index c410360..84d352a 100644
--- a/packages/web-client/public/js/physics-engine.js
+++ b/packages/web-client/public/js/physics-engine.js
@@ -1062,13 +1062,12 @@ if (typeof PhysicsEngine === 'undefined') {
       let dirX = this.state.lastDirection.x || 0
       let dirY = this.state.lastDirection.y || 0
 
+      // ИСПРАВЛЕНО: если направление (0,0), используем горизонтальное по умолчанию
+      // НЕ берём из ball.vx/vy, т.к. они могут содержать устаревшие значения
       if (dirX === 0 && dirY === 0) {
-        const sp = Math.hypot(this.ball.vx || 0, this.ball.vy || 0)
-        if (sp > 0) {
-          dirX = (this.ball.vx || 0) / sp
-          dirY = (this.ball.vy || 0) / sp
-          this.setDirection(dirX, dirY)
-        }
+        dirX = 1
+        dirY = 0
+        this.setDirection(dirX, dirY)
       }
 
       this.setVelocity(dirX * pixelsPerSecond, dirY * pixelsPerSecond)
