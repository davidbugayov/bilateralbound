{
  "name": "bilateralbound",
  "version": "1.1.0",
  "description": "A scalable real-time multiplayer game built with SOLID principles",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "build": "echo '✅ No build step required for Node.js app'",
    "build:pages": "echo '✅ Static files are served by Node.js server' && mkdir -p dist && cp -r public/* dist/ 2>/dev/null || echo 'No public files to copy'",
    "debug": "LOG_LEVEL=DEBUG node src/server.js",
    "local": "PORT=3000 npm run start",
    "dev:open": "PORT=3000 npm start & sleep 2 && open http://localhost:3000 && wait",
    "test": "npm run test:lint && npm run test:server",
    "test:server": "node -c src/server.js && echo '✅ Server syntax OK'",
    "test:lint": "echo '✅ No linter configured yet'",
    "test:full": "npm run test && npm run health-test",
    "test:all": "node run-all-tests.js",
    "test:comprehensive": "node test-comprehensive.js",
    "test:sync": "node test-latency-sync.js",
    "test:basic": "node test-basic-sync.js",
    "test:speed": "node test-simple-speed.js",
    "test:corners": "node test-physics-corner.js",
    "test:viewer-sync": "node test-viewer-sync.js",
    "test:final-verification": "node final-verification.js",
    "health-test": "sleep 2 && curl -s http://localhost:3000/health | jq .status 2>/dev/null || echo '❌ Health check failed'",
    "clean": "rm -rf node_modules package-lock.json && npm install",
    "status": "curl -s http://localhost:3000/health || echo '❌ Server not running'",
    "logs": "tail -f /dev/null",
    "quick-start": "echo '🚀 Запуск сервера...' && npm run dev:open"
  },
  "keywords": [
    "game",
    "multiplayer",
    "http-only",
    "solid",
    "scalable"
  ],
  "author": "BilateralBound Team",
  "license": "ISC",
  "type": "commonjs",
  "homepage": ".",
  "render": {
    "type": "web",
    "buildCommand": "npm install",
    "startCommand": "npm start"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "nodemon": "^3.1.10"
  }
}
