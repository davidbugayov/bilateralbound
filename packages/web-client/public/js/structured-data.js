(function () {
  'use strict'

  // MedicalWebPage structured data is now inline in index.html as JSON-LD.
  // This file is kept for backwards compatibility — it no longer injects
  // anything, but old cached HTML pages may still reference it.
})()
