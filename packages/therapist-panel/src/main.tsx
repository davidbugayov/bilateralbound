import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="p-4 text-zinc-100 bg-zinc-900 min-h-screen">
      <p className="text-sm text-zinc-400">EMDR Panel — загрузка...</p>
    </div>
  </React.StrictMode>
)
